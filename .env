# Please note that ideally .env file ignored in .gitignore since it carries sensitive data.
# It is checked in to git since there is no such data in this repo and this is a portfolio work.
PRACTICE_E2E_URL=https://bonigarcia.dev/selenium-webdriver-java
PRACTICE_API_URL=https://practice.expandtesting.com/notes/api

# Database (local Docker — run: npm run docker:up)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=notesdb
DB_USER=notesuser
DB_PASSWORD=notespassword
POSTGREST_URL=http://localhost:3000

# SauceDemo (storageState / auth-reuse demo)
SAUCE_DEMO_URL=https://www.saucedemo.com