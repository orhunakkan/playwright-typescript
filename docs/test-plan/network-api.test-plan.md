# Test Plan — Network & API (TAB1-12)

| Field        | Value                                                      |
| ------------ | ---------------------------------------------------------- |
| JIRA story   | [TAB1-12](https://orhunakkan.atlassian.net/browse/TAB1-12) |
| Lab URL      | `/practice/network-api`                                    |
| Spec         | `tests/network-api/network-api.spec.ts`                    |
| POM          | `pages/network-api.page.ts`                                |
| RTM          | `docs/rtm/network-api.rtm.md`                              |
| Generated by | `test-plan-generator` skill                                |

## 1. Scope

**In scope**

- `page.route()` handler registered before navigation intercepts the initial GET /api/notes
- `page.waitForResponse()` synchronisation after UI actions (Add note)
- POST request payload inspection via `page.waitForRequest()` to assert note text
- Simulated HTTP 500 via `route.fulfill({ status: 500 })` → UI error state rendered
- `route.fulfill({ json: [...] })` returns static JSON; notes list renders stub correctly
- Network isolation: no real requests reach the backend when route handlers are active
- Accessibility (axe) on page load and error state
- Performance budget on initial load

**Out of scope**

- Real backend persistence (tests stub or observe — they do not depend on server state)
- Auth / login flows
- CRUD coverage via `request` fixture (that is TAB1-24)
- Load and stress testing
- DELETE endpoint interception (not covered by the six ACs)

## 2. Test types

| Type                            | Applies | Notes                                                        |
| ------------------------------- | ------- | ------------------------------------------------------------ |
| Functional — positive           | ✅      | stub interception, waitForResponse, POST payload assertion   |
| Functional — negative           | ✅      | empty input gate, 500 error state, no-interception baseline  |
| Functional — boundary (BVA)     | ✅      | empty-array stub, special chars in payload, Promise.all race |
| Functional — data-driven        | ✅      | special-char payload table                                   |
| Accessibility (axe, all states) | ✅      | load + error state                                           |
| Non-functional — performance    | ✅      | navigation-timing budget (`@performance`)                    |
| Cross-browser                   | ✅      | full config matrix (Chrome, Firefox, Edge, Safari)           |

## 3. Environments & data

- **Environments:** `.env` (`BASE_URL=https://stagecraftlabs.com`); `ENV=qa|uat` selects
  `.env.qa` / `.env.uat`. CI supplies `BASE_URL` via repo variable.
- **Test data:** Fixed stub objects (`STUB_NOTES`) with controlled `id`/`text`/`createdAt`;
  faker is not needed — note text is deterministic by design so assertions are exact.
- **Auth/seed:** none required (public page, no login).
- **Isolation:** `page.route()` handlers make all P1 cases network-isolated; no side effects
  on the shared backend when route handlers are registered before navigation.

## 4. Browser / device matrix

From `playwright.config.ts` `projects[]`: **Desktop Chrome, Desktop Firefox, Desktop Edge,
Desktop Safari** (Edge→Chromium, Safari→WebKit).

## 5. Risk assessment & priority

| Area / Requirement                        | Likelihood | Impact | Risk | Priority |
| ----------------------------------------- | ---------- | ------ | ---- | -------- |
| Route registered before navigation (AC-1) | M          | H      | H    | P1       |
| waitForResponse synchronisation (AC-2)    | M          | H      | H    | P1       |
| POST payload inspection (AC-3)            | M          | H      | H    | P1       |
| 500 error state via route.fulfill (AC-4)  | M          | H      | H    | P1       |
| Static JSON stub → list renders (AC-5)    | M          | M      | M    | P1       |
| Network isolation (AC-6)                  | L          | H      | M    | P2       |
| Empty array stub → empty list state       | L          | M      | M    | P2       |
| POST payload with special characters      | L          | M      | M    | P2       |
| Accessibility on load + error states      | M          | H      | H    | P2       |
| Performance (load budget)                 | L          | L      | L    | P3       |

## 6. Entry criteria

- ✅ Requirements extracted and prioritized (6 ACs, TAB1-12)
- ✅ POM exists (`pages/network-api.page.ts`)
- ✅ App URL reachable; `BASE_URL` configured
- ✅ GET /api/notes confirmed returning `[{id, text, createdAt}]`
- ✅ Error region confirmed as `div[role="alert"]` (DOM-inspected with Chrome DevTools)

## 7. Exit criteria

- ✅ Every P1 + P2 requirement has a passing automated case
- ✅ 0 open non-flaky defects of severity ≥ High linked to TAB1-12
- ✅ Accessibility: 0 critical violations on load and error states
- ⏳ Green across the full browser matrix **in CI** (gates the move to Done)
- ✅ RTM generated and current

## 8. Deliverables

- Spec: `tests/network-api/network-api.spec.ts` (~16 cases)
- POM: `pages/network-api.page.ts`
- RTM: `docs/rtm/network-api.rtm.md`
- This plan: `docs/test-plan/network-api.test-plan.md`
- CI run: `.github/workflows/playwright.yml`

## 9. Schedule / effort (lightweight)

requirements → **test plan** → POM → spec → local run → triage/defects → RTM → In Review → CI → Done.
